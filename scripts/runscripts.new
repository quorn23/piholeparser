#!/bin/bash
## This is the central script that ties the others together

## Variables
SCRIPTDIRA=$(dirname $0)
source "$SCRIPTDIRA"/foldervars.var

## Run Logs
#if
#[[ -f $RUNLOGSCRIPT ]]
#then
#bash $RUNLOGSCRIPT
#fi

## Logo
#if
#[[ -f $AVATARSCRIPT ]]
#then
#bash $AVATARSCRIPT
#else
#echo "Deathbybandaid Logo Missing."
#fi

#echo ""
for dir in "$TOPLEVELSCRIPTSDIRNEW"*/
do

DIRNAME=$(echo `basename $dir | cut -f 1 -d '.'`)

echo "dir is $dir"
echo "dir basename is $DIRNAME"


done
